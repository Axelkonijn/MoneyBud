# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# Real financial data — this repo is public, keep it synthetic
data/real/
# MoneyBud's own data file lives in the user's profile, never here (arc42 §8.3). A second line of
# defence in case one is ever copied in.
moneybud.json
moneybud.json.tmp
moneybud.lock
*.ofx
*.qif
*.csv.private

# Raw personal notes — only the cleaned versions in docs/stakeholder/ are published
docs/transcript.txt

# Databases
*.db
*.db-journal
*.sqlite
*.sqlite3

# Node
node_modules/
dist/
build/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# .NET
bin/
obj/
*.user

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini
