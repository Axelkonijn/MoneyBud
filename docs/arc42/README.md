# Architecture Documentation — arc42

MoneyBud's architecture documentation, following the [arc42](https://arc42.org) template.

## How to read this

Sections are filled in **progressively**, not all at once. An empty section is normal and means
"we haven't needed to decide this yet" — it is not a gap to be padded out. arc42 is explicit about
this: write what carries information, leave the rest.

Each file states what belongs in it, so it's clear what's missing versus what's deliberately
still open.

## Sections

| # | Section | Purpose | Status |
|---|---|---|---|
| [01](01-introduction-and-goals.md) | Introduction and Goals | What MoneyBud does, for whom, and the top quality goals | **Filled** — including what "the first version" covers and why it is not the same span as "the first increment", and that the fifth increment, a Dutch desktop UI headed by the radial diagram, is the first with a screen and is **built** |
| [02](02-architecture-constraints.md) | Architecture Constraints | What we are not free to choose | **Filled** |
| [03](03-context-and-scope.md) | Context and Scope | System boundary; who and what it talks to | **Filled** |
| [04](04-solution-strategy.md) | Solution Strategy | The handful of decisions that shape everything else | **Filled** — the shaping decisions, now including the toolkit and a three-project decomposition, and an honest account of the quality goals. Since the UI all four have something built for them: legibility through the ring, and effortless entry partly, on desktop only. **None is measured** |
| [05](05-building-block-view.md) | Building Block View | Static structure — the boxes and what's inside them | **Filled** — three source projects (domain, a toolkit-free presentation layer, a thin Avalonia desktop) and one test project. The UI increment is the first to move a boundary ([ADR 0006](../decisions/0006-three-source-projects.md)). Storage is the one building block still absent |
| [06](06-runtime-view.md) | Runtime View | How the blocks interact for key scenarios | **Filled, one scenario** — recording an expense through the screen: two layers of refusal, the Dutch added on the way back, nothing on screen holding a figure. Every other act follows its shape |
| [07](07-deployment-view.md) | Deployment View | Where it runs | **Filled** — desktop, one process. Operating systems follow Avalonia (Windows, macOS, Linux), and only Windows has been run. Persistence and distribution still open, with no installer |
| [08](08-crosscutting-concepts.md) | Cross-cutting Concepts | Rules applying everywhere — money handling lives here | **Filled** — §8.1 maps §12's two distinctions onto the code, places income's pool outside both of them, and lists what has no code yet (accounts and everything built on them, carry-over at period opening, *Leftover*, recurring transactions; income, *Unassigned*, *Archived*, the default categories, *Assign* and *Over-assigned* have left it, built). The category increment's shape is there too. One comparer, `CategoryName`, holds the name rule; it is ordinal, so it does not depend on the machine's culture. It corrects a divergence §8.1 used to record, where names were compared exactly by accident, and the record of it is kept. The category acts and `Assign` hold "shows, never blocks" by their result types, with a clipped shortfall reported rather than absorbed. The full "shown in a period" rule is now **one domain query, `CategoriesShownIn`**, built for the UI, and the "shown" steps read the view built on it. **`Assign` is the only writer of a budget.** The `SetBudget` scaffold was deleted rather than aligned, and §8.1 says why. It also says how the two obstacles it had recorded were settled: past-period budget *Givens* are made by moving the test clock into that period and assigning, not through a test-only door (settled in the approved plan), and the income scenario that relied on budgets bypassing the pool now asserts the figure with them subtracted (approved at the scenario gate). §8.2 settles whole cents, the `Money` type, the sign convention and no currency field — unchanged by income, which those rules already covered — with storage and timezones still open. Since the UI it also covers how typed text becomes a `decimal` without rounding, why display formatting ignores the machine's culture, and why the ring's floating-point shares are not money. §8.3 defers persistence, with the trigger stated, reconsidered for the UI increment and kept; the UI keeps nothing. The first demo named keeping data as missing and "later", and §8.3 records why that is not the trigger firing. **§8.4, new, covers the presentation layer**: the period on screen held as a period, nothing cached, the once-a-minute refresh and its lag, behaviour decided while building rather than ruled, `Tekst` and the test that reads §12's display-terms table, and **how the scenarios are run** (*Given* on the ledger, every *When* through `MoneyBudApp`, *Then* about what is shown on the presentation layer), including how `type-an-amount.feature`'s steps differ, and the current test count (620). Its *decided while building* table records that the category box emptying after success is now the stakeholder's ruling. Since the first demo it also covers pointing at the ring, where the Desktop only hands over a share, and **the one test that reads the window's markup**, for the order of form fields, approved as a small departure from ADR 0006. §8.2 records the ring's 2% minimum as drawing, not money. `HasBudget` stays a watch-out: no production code uses it, and the settled rule for deleting a category is named as a second place it must not reach |
| [09](09-architecture-decisions.md) | Architecture Decisions | Index of ADRs | 6 decisions. The income, category and assigning increments added none, and §9 says why. The UI increment added two: **0005**, the toolkit (Avalonia), and **0006**, three source projects, which **supersedes decision 1 of 0004**, the first record superseded, and only in part. The first demo added none: 0006 carries a dated note that one test now reads the Desktop's markup |
| [10](10-quality-requirements.md) | Quality Requirements | Quality tree and concrete scenarios | Empty, **on purpose** — the UI exists, so it is writable, but no measure has been agreed with the stakeholder, and an invented threshold would look like a requirement that had been thought about |
| [11](11-risks-and-technical-debt.md) | Risks and Technical Debt | Known problems, named honestly | **Filled**, now with a *Resolved* table. The "shown in a period" row is resolved by the UI. So is the "2.000" problem the spec review found, and so is narrowing having lived in the Desktop: it was moved into the presentation layer, the first time the Desktop's "decides nothing" rule was enforced. The Desktop having no automated tests, by plan, is a new row. **Reading typed amounts having no scenario is resolved**: `type-an-amount.feature` was approved at the scenario gate on 2026-09-26 and is bound and green, with one approved refusal ("€ −50") held by a unit test rather than a scenario. The start-day and no-correction rows stand, the latter now noting that the first demo named it and deferred it, and that correcting entries was **settled on 2026-09-26 as the next increment**, not built, with both of the row's questions answered. The desktop-first row now notes what the UI kept open for mobile. The Desktop row notes how the first demo's three rulings were kept out of the Desktop, except the field order, which the one markup test holds |
| [12](12-glossary.md) | Glossary | Domain terms, defined once | **Filled, two open questions** — how an overdrawn account is shown, now that over budget and over-assigned carry a marker (open until accounts are built); and what happens to an income back-dated into a period that has **already been swept**, which the late-expense principle nearly but does not quite answer, and which cannot be decided until a sweep exists to decide against. The **category model** is settled and now **built**: names trimmed at the ends, compared case-insensitively with any run of inner whitespace counting as one space, and stored as typed (a correction, since made, to code that compared them exactly by accident), with a name that trims to nothing refused; adding an existing name hands back the existing category **with its existing spelling** and says so; removing a category **archives** it — out of new entry, history intact, still shown in every period where it has a budget of more than zero or an expense, the current one included, never confirmed first and announced afterwards; an archived category is **brought back**, announced, by adding its name or by recording an expense against it — the second a **decision that overturned an earlier derivation**, which had assumed recording against a name adds it when in fact it is refused — so there are two routes back and still no separate un-archive act. The **assigning model** is now **built** too: assigning allowed in the current period and later ones and refused in a past one; zero accepted and moving nothing, unlike a zero transaction; refusals reported in a fixed order; and a **positive** assignment to an archived category bringing it back as a third route, while a negative or zero one leaves it archived. Decided for later increments: an archived category's figure is not offered back when a period opens (not built); the full display rule, under which a category in use is shown in the current and later periods even without history, and a past period shows only categories with history (**built** in the UI increment); renaming deferred (since answered, below); and six Dutch **default categories** whose names are user-facing content rather than project vocabulary. Everything else holds as before — account-backed categories, the pool account, *Accumulated*, the end-of-period sweep, the expense account default, overdrawing, negative assignment against a *Budget* that floors at zero — including that an over-large one is clipped and the shortfall reported — and what a start day means in a month too short to contain it are all settled. Now also the **income model**: a required label where an expense's is optional, future-dating allowed where an expense's is not, *Unassigned* and *Left to assign* merged into one figure named *Unassigned*, *Over-assigned* named for its negative state, and net worth settled as a point-in-time figure that excludes expected income — so the two views disagree about it by design. All of that income model is now built, *Over-assigned* included, which became reachable once assigning was built ([§8.1](08-crosscutting-concepts.md)). **The user interface** is settled for the fifth increment and **built**, with the toolkit in ADR 0005: it covers what the domain does and nothing more; it steps between periods, which is where the display rule gets built; the start day stays at the 1st for now; the start screen is the Overview, headed by a **ring** of budget-sized slices filled as far as spent plus a slice for *Unassigned*, with an empty grey outline when there is neither income nor budget, and one marker beside the negative figure for over budget and over-assigned (a **revision** of "plain negative figure, unremarked", which leaves how an overdraft is shown open again); category entry is free text with suggestions, so all three routes back stay reachable; no correction of entries, accepted for the demo; it starts with the default categories and keeps nothing; and it is in Dutch, with a **Dutch display terms** table kept separate from the *Dutch source terms*. Settled while its scenarios were written: an entry's date defaults to today and assigning to the period on screen; assigning is offered in a past period and refused; an expense, income or assignment landing in another period leaves the screen where it was and says where it went; no one-step way back to the current period; category rows show *Budget*, *Spent* and *Remaining*; categories and slices run largest *Budget* first, ties in order added (a brought-back category keeping its original place), *Unassigned* last going clockwise; expenses and incomes are two lists, each newest first; the Overview is laid out income left, plan middle, expenses right, as the stakeholder's wish; and a MoneyBud left open past a period boundary stays on the period it showed, with the label lagging by up to a minute. The scenario writer's assumptions and one consequence of that last rule were **approved at the scenario gate**. Settled at or after the plan gate and review: **how a typed amount is read** (comma or point as the decimal mark, no thousands separator, and "2.000" refused as ambiguous, naming both readings), now specified by `type-an-amount.feature`, with two rulings of 2026-09-26 (four or more whole-cent digits after the mark, and a mark without a digit on each side, are not amounts) and six scenario-writer choices approved at its gate; suggestions **narrow on "contains"**, ordinally with case and whitespace runs ignored, decided in the presentation layer; each marker badge names its own state; an archived category still shown carries a *Gearchiveerd* caption; and "alphabetical" means the invariant culture's order, so "Één" sorts among the E's. **The first demo** (2026-09-26) added five rulings, now **built**: the category box empties after an entry goes through; the ring is the middle column's centrepiece; pointing at a slice shows everything its row does, in the ring's hole; every slice is drawn at least 2% of the ring while the fill stays exact, a **revision** of the exactly proportional ring, which sent `overview.feature` back through the scenario gate and added `point-at-a-slice.feature`; and each form asks what before how much. The width, the squeeze rule and the layout below the ring were approved at the plan gate. The date defaulting to today after stepping was raised and confirmed unchanged. The *Dutch display terms* table is **read by a unit test**, so editing it is a code change. **Corrections**, settled 2026-09-26 for the next increment and **not built**: an expense or income can be changed or removed in any period, past ones included; removing **asks first**, the one act that does, because it destroys a record (the principle that leaves archiving unconfirmed, applied the other way); a change is judged as if recorded now, overwrites the entry and keeps no history; removing or lowering an income may leave its period over-assigned, shown with the marker; a changed date that moves an entry elsewhere leaves the screen where it was; an entry is corrected by clicking its row, in the entry form's *Wijzigen* state. A category can be **renamed** (a taken name refused, its own name respelled allowed, past periods showing the new name, an archived one staying archived), and one with **no history in any period** can be **deleted**, unconfirmed, as a separate act from archiving. Corrections that lower a swept period's spending **join** the open sweep question. Two points settled later the same day: fixing an expense **already on** an archived category does not bring it back (only moving one onto it does), and a past period left over-assigned by a correction stays that way, accepted as "past is past". Ruled while the scenarios were written: a change and a rename are announced afterwards; saving an unchanged entry is never refused and goes through quietly, so a loaded amount must be one the amount box accepts; declining the removal question is met with silence; and Axel's own reason for deleting is recorded: so the name is free again, since an archived category keeps its name. **The four corrections feature files were approved at the scenario gate on 2026-09-26**, and with them six readings that now stand as decisions: a changed entry and a renamed category keep their places, a renamed category's old name is free, a deleted-then-re-added category goes last, moving an income out can leave a period over-assigned, and "no history" means no history now. That a removal is announced afterwards was approved with them. The Dutch words are approved (*Wijzigen*, *Verwijderen* for both removing an entry and deleting a category, *Hernoemen*) but **held out of the table** until the build, because adding a row fails `TekstTests` |

## Where this connects

Section 10's quality scenarios and the [Gherkin features](../../features/) describe different
things and should not be confused. Gherkin covers **functional** behaviour — what the user can do.
Section 10 covers **quality** attributes — how well, how fast, how safely.
